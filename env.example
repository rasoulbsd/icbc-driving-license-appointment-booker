# Backend Configuration
PORT=3000
EXAM_TYPE=7-R-1 #5-R-1
LAST_NAME=
BCID_NUMBER=111556588
ICBC_DATE=14 March 2024
APPOINTMENT_SEARCH_DAYS=14
ICBC_KEYWORD=2336lili
ICBC_COOKIES=TS018e4505=...; TS3c41ca9e027=...; JSESSIONID=...

# Playwright Configuration (for puppeteer-version branch)
PLAYWRIGHT_HEADLESS=false  # Default: 'false' (GUI visible). Set to 'true' for headless mode

# Bot Configuration
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHANNEL_ID=-1000000000000
TELEGRAM_CHANNEL_ID_ALL=-1000000000000

# Bot Mode: 'dev' or 'production' (defaults to 'production')
BOT_MODE=production  # Set to 'dev' or 'development' for development mode

# API URLs - Mode-specific (recommended)
# Development mode URLs (used when BOT_MODE=dev)
DEV_API_URL=http://localhost:3000/appointments
# DEV_API_URL_ALL=http://localhost:3000/appointments-all  # Optional, auto-derived if not set
# DEV_API_URL_SEARCH=http://localhost:3000/appointments/search  # Optional, auto-derived if not set

# Production mode URLs (used when BOT_MODE=production)
PROD_API_URL=http://localhost:3000/appointments
# PROD_API_URL_ALL=http://localhost:3000/appointments-all  # Optional, auto-derived if not set
# PROD_API_URL_SEARCH=http://localhost:3000/appointments/search  # Optional, auto-derived if not set

# Legacy API URLs (for backward compatibility - only used if DEV_API_URL/PROD_API_URL not set)
# API_URL=http://localhost:3000/appointments
# API_URL_ALL=http://localhost:3000/appointments-all
# API_URL_SEARCH=http://localhost:3000/appointments/search

# Backend connection (optional - auto-detected if not set)
# BACKEND_HOST=backend  # Use "backend" for Docker, "localhost" for local
# BACKEND_PORT=3000

LOCATION_SEARCH=north vancouver
USE_PLAYWRIGHT_API=true  # Set to 'true' to use Playwright-based API, 'false' for traditional API
DEBUG_MODE=false
UPDATE_INTERVALS=1,1.25,1.5,1.75,2